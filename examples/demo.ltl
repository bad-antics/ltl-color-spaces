// examples/demo.ltl — runnable demo for ltl_color_spaces

import std::io
use ltl_color_spaces as lib

fn main() {
    io::println("ltl_color_spaces demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}