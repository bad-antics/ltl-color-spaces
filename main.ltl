// ltl-color-spaces/main.ltl
//
// sRGB / Lab / LCh / XYZ colour-space conversions.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-color-spaces v0.1.0")
    io::println("sRGB / Lab / LCh / XYZ colour-space conversions.")
    0
}
